上交所-大数据组-应用项目-模拟数据
echo "# template-vue-cli" >> README.md
第一次：
git init
git add text.txt
git commit -m "first commit"
git remote add origin https://github.com/zghgit/template-vue-cli.git
git push -u origin master

之后新增文件
git add .
git commit -m "备注"
git push -u origin master
